import React from 'react';

const Projects = (): React.ReactElement => {
    return (
        <div>
            <p>This is Projects...</p>
        </div>
    );
};

export default Projects;
