import React, { useState } from 'react';
import './Xgeo.css';
import Brazil from './brazil/Brazil';

const Xgeo = (): React.ReactElement => {
    return (
        <Brazil />
    );
};

export default Xgeo;
